// TODO!

/*
use rocket::serde::{Serialize, Deserialize};

#[derive(Debug, Serialize, Deserialize)]
pub struct Claims {
    exp: usize,
    iat: usize,
    iss: String,
    sub: String,
}

#[derive(Debug)]
pub struct JWT {
    pub claims: Claims
}
*/
